# AriadneMultiplexer 実装計画

更新日: 2026-09-14

## 1. 目的

本書は、次の設計書で定義した内容を実装に移すための、意思決定済みの実装順序と完了条件を定義する。

- `AriadneMultiplexer 設計書 v0.2.md`
- `追加変更要求設計書.md`
- `streammux要求実装差異.md`

実装は streammux の API 確定、Ariadne Core、daemon、CLI、Plugin/Label 基盤、フルスクリーン TUI の順に進めた。今後の GUI/CUI 拡張でも Core のドメインモデルを共有し、表示方式を frontend 内へ閉じ込める。

### 1.1 Go package構成

Ariadneは現時点でGo SDKを外部公開しないアプリケーション専用moduleとする。再利用可能な通信libraryはstreammuxに分離済みのため、Ariadne本体の実装は`internal/`へ置く。

- `cmd/ariadne`: CUIとdaemon modeを選択する唯一の薄いentrypoint
- `internal/frontend/cui`: CLI、attach、terminal入力・resize、TUI起動
- `internal/frontend/tui`: layout、VT画面合成、status bar、ANSI描画backend
- `internal/app/daemon`: daemon entrypointの起動・終了制御
- `internal/core`、`internal/daemon`、`internal/client`、`internal/protocol`: GUI/CUIで共有する本体機能
- `internal/config`、`internal/statefile`、`internal/plugin`、`internal/platform`: 共通の内部実装
- `integration`: executable境界の実PTY/ConPTY E2E

GUI版は将来`cmd/ariadne-gui`と`internal/frontend/gui`を追加し、CUIと同じ`internal/client`/`internal/protocol`を介してdaemonへ接続する。外部Plugin ABI/SDKは方式確定後に別のversion付き境界として追加し、現在の内部型を先にpublic APIとして固定しない。

---

## 2. streammux の先行作業

状態: **完了**（AriadneのAPI baseline: streammux tag `v0.3.2`）

streammux は Ariadne が必要とする基本機能と終了Session保持policyを `v0.2.0` で確定し、`v0.3.0` で Windows ConPTY backend、`v0.3.1` でhistory replayとlive attachment queueの分離、`v0.3.2`で有界受信queueのbackpressure policyを追加した。Ariadne は macOS/Linux の Unix PTY と Windows の ConPTY を同じ `pty.ManagedFactory` 境界で使用し、dependency を `v0.3.2` に固定する。

### 2.1 終了セッションの保持

- `pty.ManagerConfig.SessionLifecycle` へ明示的な終了後policyを設定できるようにする。streammuxの既定値は `ExitedSessionRemove`、Ariadneは `ExitedSessionRetain` と `MaxRetainedSessions = 64` を指定する。
- 異常終了した Session を即座に削除せず、history と `ExitStatus` を保持できるようにする。
- 終了後の `Attach` は history を再送し、最後に `Exit` を通知する。`Input` と `Resize` は拒否する。
- TTL は設けない。64件を超えた場合は、最も古い終了 Session から退避する。既存の Session 単位と Manager 全体の history byte 上限も併用する。
- `Manager.Remove(StreamID)` を追加する。終了済み Session だけを削除でき、実行中の Session には `ErrSessionRunning` を返す。

### 2.2 attachment の制御と観測

- `pty.ManagerConfig.MaxAttachmentsPerSession` は必ず正の上限を要求し、Ariadne では `1` とする。
- Attach 上限の判定は Manager 内部で原子的に行い、複数 connection からの競合を防ぐ。
- `SessionInfo` へ `AttachmentCount`、`ExitStatus`、`HistoryAvailable` を追加する。
- Manager event へ `Attached`、`Detached`、`Retained`、`Evicted`、`Removed` を追加する。`Detached` は明示 detach、connection 切断、送信失敗などの理由を持つ。

### 2.3 streammux 完了条件

- 終了後の replay、Exit 通知、Remove、古い Session の退避を単体テストで確認する。
- 同時 Attach 時に一方だけが成功することを race test で確認する。
- macOS/Linux の実 PTY テスト、`go test -race ./...`、`go vet ./...` を通す。
- API と設計書を同期し、Windows ConPTY backend、bounded replay、受信backpressureを含む `v0.3.2` を使用する。

---

## 3. Ariadne Core

状態: **Phase 2 完了**

`core` package に、型付き ID、`default/main` 初期階層、N 分木 Pane layout、単一 executor、Command/Event、原子的な snapshot + subscribe、有界 frontend Event queue、connection 単位の一時 focus を実装した。永続化、PTY lifecycle、daemon I/O は後続 Phase に分離している。

### 3.1 基本構成

- Go module は `github.com/aruzen/ariadne` とする。
- streammux `v0.3.2` を通常の module dependency として使用する。
- 状態変更は単一の Core executor goroutine へ集約する。
- PTY 生成、socket I/O、状態ファイル書込みは executor の外で実行する。I/O 開始前に予約状態を作成し、完了結果を executor へ戻して確定する。
- Core/daemon service は `pty.Manager`、`pty.Session`、`streammux.StreamID` を直接使用する。不要な runtime port adapter は作らない。

### 3.2 ID、階層、Pane 配置

- `WorkspaceID`、`WindowID`、`PaneID` は別々の型を持つ `uint64` とする。
- ID は単調増加させ、次の採番値を状態ファイルへ保存する。
- `TerminalID` には streammux の `StreamID` をそのまま使用する。TerminalID は daemon 実行中だけ有効とする。
- daemon 起動時に `default` Workspace と `main` Window を必ず作成する。
- Workspace 名は全体で一意、Window 名は Workspace 内で一意とする。Pane/Terminal タイトルは重複可とする。
- Terminal 作成先の既定値は `default/main` とする。
- Pane 配置は N 分木とし、各分岐が方向と整数比率を持つ。
- 同方向の split は兄弟として挿入し、全兄弟の比率を均等化する。異方向の split は新しい分岐で包む。
- 最後の Pane が削除されても Window と Workspace は残す。

### 3.3 Command / Event

- Core の Command/Event 基盤は最初に汎用的に実装し、Terminal、Workspace、Window、Pane、Label の拡張を同じ境界で受けられるようにする。
- Core Event に単調増加する `Revision` を付与する。Event 自体はメモリ内だけに保持し、永続化はドメイン状態と分離する。
- frontend ごとに有界の Event queue を持つ。queue が溢れた場合はその connection を切断する。
- frontend は再接続後に状態 snapshot とその Revision を取得し、以降の Event を購読する。snapshot 作成と subscriber 登録は executor 上で原子的に行う。
- focus や表示選択は frontend connection ごとの一時状態とし、daemon 全体の状態や状態ファイルには含めない。

---

## 4. daemon / frontend 通信

状態: **Phase 4 完了**

`daemon`、`protocol`、`platform/localipc`、`ariadne daemon serve` を実装した。配布物は`ariadne`単一binaryとし、同じ実行ファイルをfrontend processとdaemon processの二つのroleで起動する。1 connection につき1つの `streammux.Peer` を所有し、Ariadne JSON Command/Event と PTY protocol を同一 stream 上に登録する。PTY の Open/List/Inspect/Kill は拒否し、Attach/Detach/Input/Resize は Core 所有 Terminal に限定する。macOS/Linux は owner・mode・peer UID・stale 判定付き Unix socket、Windows は current user SID のみに限定し接続先/接続元processのSIDも照合する Named Pipe を使用する。正常終了は Command を quiesce して状態を flush した後、connection、PTY Manager、local IPC endpoint の順に解放する。

### 4.1 streammux 上の protocol

local IPC connection 一本ごとに `streammux.Peer` を一つ作り、次を同一 connection 上で多重化する。

- Ariadne Command request/response: JSON
- Ariadne Core Event: JSON
- PTY Input/Output: raw bytes
- PTY control、Replay、Exit: streammux `pty.Protocol`

MessageType は次の範囲を予約する。

- `0x1000–0x10ff`: Ariadne Command/Event
- `0x1100–0x11ff`: PTY protocol

CorrelationID は streammux Header の値を使用し、JSON 内に重複して持たせない。PTY frame だけが StreamID を TerminalID として使用する。

JSON response は成功値または安定した error code を返す。少なくとも次を定義する。

- `invalid_argument`
- `not_found`
- `already_attached`
- `invalid_state`
- `permission_denied`
- `internal`

PTY の Open/List/Inspect/Kill は汎用 `pty.Protocol` 経由では許可しない。Ariadne Command handler が Core を経由して `pty.Manager` を直接操作する。`pty.Protocol` は Attach/Detach/Input/Resize/Output/Replay/Exit に使用する。

### 4.2 daemon lifetime

- daemon はユーザー単位で一つとし、複数 frontend connection を受け入れる。
- 通常の CLI command は daemon が未起動なら自動起動し、最大3秒間 ready を待つ。`daemon status` と `daemon stop` は自動起動しない。
- macOS/Linux の socket は `XDG_RUNTIME_DIR/ariadne/ariadned.sock` を優先し、fallback でユーザー固有の `TMPDIR/ariadne/ariadned.sock` を使用する。directory は0700、socket は0600とし、接続元 UID を検証する。
- Windows は `\\.\pipe\ariadne-<SID>` を既定 endpoint とする。Pipe ACL、server process、client process がいずれもcurrent user SIDに属することを検証し、remote clientを拒否する。
- endpoint の bind 競合を利用して多重起動を防ぐ。Unix socketでは所有者とdaemon不在を確認できたstale socketだけを削除し、Named Pipeはhandle解放により消滅させる。
- foreground daemonは`ariadne daemon serve`で起動する。通常commandは`os.Executable()`で同じbinaryをdaemon modeとしてdetached起動する。別配布物や`PATH`探索には依存しない。
- `daemon stop` は実行中 Terminal があれば拒否する。`--force` の場合だけ終了させる。
- 正常終了時は、新規受付停止、状態保存、connection 終了、Manager 終了、socket 削除の順とする。二回目の終了 signal は強制終了とする。

---

## 5. CLI

状態: **Phase 5 / Phase 6 完了**

`ariadne` command と共有 `client` package を実装した。通常 command は同じ`ariadne` binaryをdaemon modeで必要時に自動起動し、最大3秒readyを待つ。`new`、`open`、`attach`、`list [--json]`、`kill`、`dismiss`、`restart`、`daemon status`、`daemon stop [--force]` は全て同一streammux connectionを使用する。`init`はdaemonやIPCに依存せず設定テンプレートを配置する。Terminal生成・再起動はCore予約、PTY起動、TerminalID確定の順に行い、起動失敗も再試行可能なPaneとして保持する。

CLI parser は標準ライブラリの `flag` を使用する。command argv は `--` 以降から受け取る。

引数なし、`-h` / `--help`、`help [command [subcommand]]`、各commandの`--help`はdaemonや設定ファイルへ接続せずUsageを表示する。未知commandと不正なglobal optionはトップレベルhelpへの導線を返す。`--`以降の`--help`は起動対象commandのargvとして扱う。

| Command | 動作 |
| --- | --- |
| `ariadne init` | 探索先へコメント付き`config.toml`を新規作成する（既存ファイルは上書きしない） |
| `ariadne new -- <command...>` | Terminal を作成し、attach せずに終了する |
| `ariadne open -- <command...>` | Terminal を作成して attach する |
| `ariadne attach <terminal-id>` | 実行中または保持中の Terminal へ attach する |
| `ariadne restart <pane-id>` | placeholder/終了 Pane を新しい TerminalID で再起動する |
| `ariadne kill <pane-id>` | 実行中 Terminal を終了し、Pane と Session を削除する |
| `ariadne dismiss <pane-id>` | 保持中の終了 Pane と Session/history を削除する |
| `ariadne list [--json]` | Pane と Terminal の一覧を出力する |
| `ariadne daemon serve` | foreground daemon processとして起動する（通常は自動起動） |
| `ariadne daemon status` | daemon の状態を表示する |
| `ariadne daemon stop [--force]` | daemon を終了する |

`list` の human-readable 出力は `PANE`、`TERMINAL`、`STATE`、`ATTACHED`、`COMMAND`、`CWD` を表示する。Terminal 未割当は `-` とする。

### 5.1 attach 動作

状態: **Phase 6 完了**

`open`と`attach`を実装し、PTYのReplay/Output/Exit event、Input、Resize、DetachをAriadne Commandと同一のstreammux connectionへ接続した。attach中だけ端末をraw modeにし、通常終了、detach、通信error、終了signalの全経路で復元する。`SIGWINCH`はResizeへ変換し、既定の`Ctrl-A d`、未知suffixの透過、`Ctrl-A Ctrl-A`によるliteral prefixを実装した。実TTYでdetach後もprocessが継続し、再attach時にhistoryが再生され、正常exit後にPane/Sessionが解放されることを確認済み。

- attach 元は TTY 必須とする。
- terminal を raw mode へ変更し、全ての終了経路で必ず元に戻す。
- macOS/Linux は `SIGWINCH`、Windows はconsole window sizeの変化を PTY Resize へ変換する。
- `Ctrl-C`、`Ctrl-D` は CLI 自身の操作に使わず PTY へ転送する。
- detach key の既定値は `Ctrl-A d` とし、TOML で変更可能とする。
- prefix 後の未知 key は prefix とともに PTY へ転送する。`Ctrl-A Ctrl-A` でリテラルの `Ctrl-A` を送信できるようにする。
- Windowsではinput/output console modeをattach中だけVT対応raw modeへ変更する。Win32 input modeの`CSI ... _` recordを認識してdetachを判定し、それ以外のrecordは子ConPTYへbyte単位で透過する。

### 5.2 process 起動情報

- command は明示 argv、設定の shell、OS既定shellの順で決定する。macOS/Linuxは`$SHELL`、`/bin/sh`、Windowsは`COMSPEC`、`cmd.exe`を使用する。
- CWD の既定値は CLI の現在 directory とする。
- 環境変数は CLI から daemon へ渡すが、log、状態ファイル、error 文には出力しない。
- 初期 terminal size は CLI の TTY size を使用し、取得できない場合は80x24とする。
- Windows daemon は `CREATE_NEW_PROCESS_GROUP | DETACHED_PROCESS` でconsoleから分離して起動する。

### 5.3 VT engine の静的リンク

状態: **Phase 8 完了**

VT parser/rendering engineにはlibghostty-vtを使用する。Ghostty source revisionとZig versionをrepositoryで固定し、生成したarchive自体はcommitしない。`go run ./tools/build-libghostty-vt`で対象platform用のheaderとstatic archiveを`build/libghostty-vt/<goos>-<goarch>`へ生成してから、AriadneをCGO buildする。

- Ghostty revisionは`third_party/ghostty/REVISION`、Zigは`0.16.0`に固定する。
- 対象はmacOS arm64/amd64、Linux amd64、Windows amd64とする。
- Windowsは`GHOSTTY_STATIC`を定義し、DLL importを無効化する。
- libghostty-vt固有のC APIは`internal/vt/libghostty`内へ隔離する。
- 配布物は引き続き`ariadne`単一binaryとし、libghostty-vtの共有libraryを要求しない。

### 5.4 フルスクリーン TUI

`ariadne tui`を追加し、現在選択中Windowの全PaneをCoreのN分木layoutどおりに表示する。各Paneは独立したPTY attachmentとlibghostty-vt stateを持ち、history replayと継続出力を同じ描画経路へ入力する。

- frontendは`Sync`で得たSnapshotへrevision順のCore Eventを適用する。欠落や順序違反は古い状態の表示を継続せずconnection errorとする。
- `libghostty` adapter、frontend共通のCell/Surface、layout compositor、ANSI backendを分離する。status barはPTYやlibghosttyに依存しない`StatusWidget` interfaceで構成する。
- PTY出力はlibghostty-vtへ入力し、色・文字属性・wide cell・cursorをSurfaceへ合成する。端末照会によってlibghostty-vtが生成した応答byteは元のPTYへ返す。
- 描画要求はdirty flagへ集約し、最大60fpsでframeを出力する。時計表示は1秒ごとに更新する。
- 初期focusは実行中Paneを優先する。`Ctrl-A h/j/k/l`で方向移動、`Ctrl-A d`でTUI終了、`Ctrl-A Ctrl-A`でliteral prefixを送信する。
- 端末resizeはlayout再計算と各PTY Resizeへ反映する。正常終了、signal、通信errorの全経路でraw mode、cursor、alternate screenを復元する。
- status barの既定widgetはWorkspace/Window、Pane、状態または操作案内、時計とする。将来のPlugin widget、theme、GUI rendererはSurface/StatusWidget境界の外側を変更せず追加する。
- Pane種別からstatefulな`paneContent`を生成する`paneRenderer`と、border/title/content領域を決める`paneChrome`を分離する。Coreにはfrontend非依存の`PanePresentation`として`auto`、`border`、`none`だけを保持し、TUI/GUIはそれぞれのrenderer既定値を選べるようにする。Terminal contentだけが任意interface経由でPTY/VT stateを使用し、ToolPaneや将来の特殊Paneは独自状態・描画・入力処理をregistryへ追加可能にする。
- `new`/`open`の`--chrome auto|border|none`をTerminal Pane生成時の表示hintとして保存し、移動・再起動・daemon再起動後も維持する。
- TUI全体のPane枠は`full`、`split`、`none`から選択可能にする。`split`では外周枠を描かず、layoutの各split nodeが兄弟間に1セルのseparatorを所有する。単一Paneは全領域を使用し、入れ子のseparator交点は罫線を合成する。`[tui].pane_frame`または`tui --pane-frame`で選択し、Pane側の明示的な`border`/`none`指定は全体設定より優先する。

---

## 6. Terminal 終了と永続化

状態: **Phase 3 完了**

`statefile` package に version 付き JSON、runtime 情報を除外する明示 schema、破損・未知 version の退避、atomic rename、100ms debounce、即時 flush を実装した。起動中 Terminal は保存時に placeholder へ変換し、非0終了・signal・PTY I/O 異常は起動情報と終了理由を保持する。`config` package は未知 key と型不一致を拒否する startup-only strict TOML loader とした。`restart` command への接続は CLI 実装 Phase で行う。

### 6.1 終了時の扱い

| 終了理由 | Pane | streammux Session/history |
| --- | --- | --- |
| exit code 0 | 削除 | 削除 |
| Ariadne の明示 kill | 終了確認後に削除 | 削除 |
| 非0 exit | 保持し error Label を付与 | 保持 |
| signal 終了 | 保持し error Label を付与 | 保持 |
| PTY I/O 異常 | 保持し error Label を付与 | 保持 |
| `dismiss` | 削除 | 削除 |

streammux が保持上限により Session を退避した場合も、Ariadne の Pane、終了理由、起動情報は残し、history 利用不可として表示する。

### 6.2 状態ファイル

- 永続化形式は version 付き JSON とし、TOML 設定と分離する。
- 100ms debounce と一時ファイルからの atomic rename で書き込む。正常終了時は即時 flush する。
- Env、PTY history、実行中の TerminalID、frontend ごとの focus は保存しない。
- daemon 再起動時、以前実行中だった Pane は TerminalID を持たない placeholder として復元する。
- 異常終了 Pane は、終了理由と起動情報を保ったまま復元し、`HistoryAvailable=false` とする。
- `restart <pane-id>` は保存済み argv/CWD と実行時の CLI 環境を用い、新しい TerminalID を割り当てる。
- 未知 version または破損 JSON を検出した場合は元ファイルをタイムスタンプ付きで退避し、既定状態を新規作成する。
- Windowsの既定状態保存先は`os.UserCacheDir()`配下とし、`XDG_STATE_HOME`が明示された場合は全OSでその指定を優先する。

### 6.3 設定

- 設定形式は strict TOML とする。未知 key や型不一致は起動 error にする。
- runtime reload は行わない。daemon側の設定変更にはdaemon再起動、TUI表示設定の変更にはTUI再起動を必要とする。
- PTY履歴・読み取りbuffer・attachment/observer queueは`[terminal]`、streammuxのframe・write/outbound/per-stream queueは`[transport]`で上限を変更可能にする。未指定時はlibrary既定値を使用し、不正値やper-stream上限が全体上限を超える設定は起動時に拒否する。

```toml
[tui]
pane_frame = "full" # full, split, none

[terminal]
history_bytes = 16777216
max_total_history_bytes = 536870912
read_buffer_bytes = 32768
attachment_queue_bytes = 4194304
observer_queue_bytes = 4194304

[transport]
max_frame_bytes = 4194304
write_queue_frames = 64
outbound_queue_bytes = 16777216
outbound_queue_frames = 4096
per_stream_queue_bytes = 1048576
per_stream_queue_frames = 256
```

---

## 7. Plugin / Label 基盤

状態: **Phase 7 完了（組み込み拡張基盤）**

CoreにWorkspace/Window/Paneを対象とするsource所有Label、設定・個別解除・source一括解除Command/Eventを追加した。現在のPlugin Hostは外部Plugin SDKではなく、静的linkされた組み込みGo拡張基盤とする。Pluginごとに独立した有界Core Event購読と逐次callback実行系を持つ。queue overflow、panic、callback timeout、規定回数の連続errorでは対象Pluginだけを無効化し、source gateを閉じた後にそのPlugin由来Labelを全削除する。Labelはruntime派生状態として状態ファイルへ保存せず、daemon起動時に異常終了Terminalのsystem error Labelを再構成する。

- 初期 Plugin は静的リンクした in-process Go interface とする。
- Plugin は Core Event を購読し、自分を source とする Label の設定と解除を行える。
- raw event queue が溢れた場合は対象 Plugin だけを無効化し、PTY drain や Core executor を block しない。
- panic または継続的な error が発生した場合も対象 Plugin だけを無効化し、その Plugin 由来の Label を全て削除する。daemon と他の Plugin は継続する。

---

## 8. 実装順序

1. [x] streammux の終了保持、`Remove`、Attach 上限、観測 event を実装する。
2. [x] streammux のテストと API document を完成させ、`v0.2.0` を確定する。
3. [x] Ariadne の ID、階層モデル、Core executor、Command/Event を実装する。
4. [x] JSON 状態の load/save、placeholder 復元、strict TOML 設定を実装する。
5. [x] daemon、Unix socket、Peer 登録、PTY Manager 連携を実装する。
6. [x] `new`、`list`、`kill`、`dismiss`、`restart` を実装する。
7. [x] raw terminal 制御を含む `open`、`attach`、detach、resize を実装する。
8. [x] Label/Plugin 基盤を Core Event へ接続する。
9. [x] 実装済み API に合わせて`実装計画.md`を同期する。他のMarkdownは管理・commit対象外とする。
10. [x] streammux `v0.3.2`、Named Pipe、ConPTY、Windows console raw/resizeを接続し、Windows実機E2Eを通す。
11. [x] libghostty-vtのrevision/build tool/CGO境界を固定し、macOS/Linux/Windowsで静的リンクする。
12. [x] libghostty-vtへPTY出力を入力し、VT stateからpaneを描画するフルスクリーンTUIを実装する。
13. [x] TUI描画をlatest-frame方式の非同期writerへ分離し、端末出力の停止中もPTY eventをdrainする。
14. [x] Pane chromeと内容rendererを分離し、borderless PaneとPTYを持たない特殊Paneを追加できるTUI拡張境界を実装する。

---

## 9. テスト計画

状態: **完了**

Core・protocol・daemon・CLI・Plugin・永続化の単体/race testに加え、12個のCLIによるdaemon同時自動起動、実Unix socket/Named Pipeでの同一user検証、daemon再起動後のplaceholder復元、実PTY/ConPTYでのopen/detach/reattach/resizeを統合テスト化した。GitHub ActionsはmacOS/Linux/Windowsでrace test、vet、実daemon/PTTY E2Eを実行する。streammux固有のAttach競合、replay連続性、受信backpressure、終了Sessionの保持・退避、Windows ConPTY lifecycleはstreammux `v0.3.2`のテストで担保する。TUIの非同期writerは、write停止中のlatest-frame置換とwrite error伝播を単体テストする。

### 9.1 Core / protocol

- ID 採番、階層、N 分木 split、終了状態遷移、Revision 順序の単体テスト。
- JSON Command の正常系、未知 operation、不正型、権限拒否、安定 error code のテスト。
- `net.Pipe` を使った Command/PTTY 多重化、再接続、Event queue 溢れ、snapshot 再同期のテスト。
- 同一 Terminal への同時 Attach で一方だけが成功する競合テスト。
- replay 中に新規 output が発生しても欠落・重複しないテスト。

### 9.2 lifetime / persistence

- exit code 0、明示 kill、非0 exit、signal、PTY I/O 異常それぞれの Pane 保持規則を検証する。
- 異常終了後の再 Attach、dismiss、64件超過時の古い history 退避を検証する。
- 状態ファイルの atomic save、debounce、破損退避、未知 version、placeholder 復元を検証する。
- Env、history、TerminalID、focus が永続化されないことを検証する。

### 9.3 daemon / CLI / PTY

- daemon 自動起動競合、stale socket、UID 拒否、`stop`、`stop --force` を検証する。
- `new`、`open`、detach、reattach、resize、restart、kill、dismiss の CLI integration test を用意する。
- macOS/Linux CI で実 PTY、Windows CIで実Named Pipe/ConPTY E2Eを実行する。
- CIで固定versionのZigからlibghostty-vtを生成し、共有libraryなしでAriadneをbuild/testする。
- TUIのlayout比率、入力prefix、focus選択、status bar切詰め、ANSI cursor、VT照会応答を単体テストで検証する。
- macOSで複数Paneの描画と入力を確認し、Linux/Windows実機でalternate screenへの進入・描画・終了時復元を確認する。
- `go test -race ./...` と `go vet ./...` を必須とする。

---

## 10. 初期実装の完了条件

- 複数 CLI connection から一つの daemon が管理する複数 Terminal を操作できる。
- `ariadne open` から raw PTY を操作し、detach、reattach、history replay、resize、終了まで動作する。
- connection 切断後も Terminal process と output drain が継続する。
- 正常終了、明示 kill、異常終了、dismiss の規則が設計どおりに適用される。
- daemon 再起動後に Pane 配置と起動情報が復元され、`restart <pane-id>` で新しい Terminal を開始できる。
- slow frontend や異常 Plugin が PTY drain、Core executor、他 connection を停止させない。
- `ariadne tui`で複数PaneのVT画面、focus、cursor、status barを表示し、Core Eventと端末resizeへ追従できる。
- GUI、Plugin status widget、theme、差分描画をCore/Event/Pane配置モデルやlibghostty adapterを作り直さずに追加できる。

---

## 11. 今後の実装ロードマップ

状態: **Phase 9〜16実装済み**

本章は初期実装完了後のPhase 9以降を定義する。前章までに記載した終了規則や操作体系は現行実装の説明であり、本章に変更予定が明記された項目は、該当Phaseの完了時に本章の規則へ置き換える。

実装順はTUIの基本操作、scrollback/clipboard、stash、ToolPane、agent attention、描画・設定の成熟、GUI/外部Pluginとする。GUIと外部Plugin ABIは最も優先順位を低くし、Core、ToolPane、daemon protocolが安定するまで固定しない。

### 11.1 共通方針

- TUI keybinding、CLI、将来のGUIは同じCore Commandとdaemon protocolを使用し、frontendごとに操作ロジックを再実装しない。
- Workspace、Window、Pane、layout、Terminal lifecycleの共有状態はCoreが所有し、focus、zoom、copy mode、検索位置などの表示状態はfrontendごとに所有する。
- layout変更は状態ファイルへ保存し、接続中の全frontendへCore Eventとして通知する。画面寸法が異なるfrontendは同じ比率を各自の表示領域へ適用する。
- 新しい設定はstrict TOMLへ追加し、未知値を起動時に拒否する。秘密情報、clipboard内容、PTY historyは設定・状態ファイル・logへ保存しない。
- 設定ファイルは`$ARIADNE_CONFIG_PATH/config.toml`、`$XDG_CONFIG_HOME/ariadne/config.toml`、`~/.config/ariadne/config.toml`の優先順で全OS共通に探索する。ファイルが存在しない場合は作成せず、組込み既定値で起動する。
- 各PhaseはmacOS、Linux、Windowsの単体/race test、必要な実PTY/ConPTY E2E、`go vet ./...`、静的link buildを完了条件に含める。

### 11.2 Phase 9: TUI操作とTerminal lifecycle

状態: **実装済み**

表示中心のTUIを日常利用可能なmultiplexerへ拡張する。command prompt、確認dialog、mode、keymap dispatchを先に共通化し、後続Phaseのcopy mode、clipboard確認、stash、ToolPaneでも再利用する。

#### Pane / Window / Workspace操作

- TUIからPaneの分割、終了、移動、resizeを行えるようにする。
- WorkspaceとWindowの作成、選択、rename、削除、およびPaneのWindow間移動を追加する。
- Coreへ不足しているrename、削除、split weight変更のCommand/Eventを追加し、protocol、client、状態ファイルへ接続する。
- split nodeへ永続的な`SplitID`を付与する。TUIの1セルresizeと将来GUIのseparator dragは同じsplit更新Commandを使用する。
- resize元frontendは現在のセル寸法から新しいweightを計算し、対象`SplitID`と新weightをCoreへ送る。他frontendでは同じweight比として表示する。
- Paneの最小表示寸法を下回るresizeは変更せず、status barに理由を表示する。

#### focus / zoom / resize

- `focus`は入力対象となるactive Paneの変更を意味する。
- `zoom`はactive Paneだけを一時的に全画面表示する。zoom状態はfrontend固有とし、Core Event、状態ファイル、他frontendへ共有しない。
- zoom中も非表示PaneのPTY outputを継続してdrainし、VT stateを更新する。
- zoom対象Paneの削除・stash、Window切替、Workspace切替ではzoomを解除する。
- 入れ子layoutのresizeは、指定方向に隣接領域を持つ最も近い祖先splitを変更し、active Pane側を1セル拡張する。

既定keybinding:

```text
Ctrl-A h/j/k/l          active Paneを左/下/上/右へ移動
Ctrl-A Ctrl-H/Ctrl-J/
       Ctrl-K/Ctrl-L    active Paneを左/下/上/右へ1セル拡張
Ctrl-A z                zoomを切り替える
Ctrl-A Ctrl-A           literal Ctrl-AをPTYへ送信する
Ctrl-A % / Ctrl-A "     左右 / 上下にTerminal Paneを分割
Ctrl-A x                active Paneを確認後に終了
Ctrl-A r                終了Paneをrestart
Ctrl-A c / n / p        Window作成 / 次 / 前
Ctrl-A ) / (            次 / 前のWorkspace
Ctrl-A :                command prompt
Ctrl-A , / $            Window / Workspaceをrename
```

command promptは`:help`で一覧を表示し、Tab補完、Up/Downまたは`Ctrl-P`/`Ctrl-N`の履歴、`Ctrl-U`の行消去、`Ctrl-W`の単語消去を提供する。引数はshellを起動せずにsingle/double quoteとbackslashを解釈し、空引数も保持する。

`:` commandはkeybinding相当のfocus、resize、split、zoom、copy、paste、Window/Workspace操作に加え、Pane IDを指定するclose/restart/run、stash/restore/preview、ToolPane、attention、diagnostics、detachを提供する。`split h|v -- command...`と`run [PANE --] command...`は指定したargvをshell経由せず実行する。

未割当のprefix sequenceは従来どおりprefixを含めてPTYへ転送する。stashとcopy/pasteのbindingは後続Phaseの節に記載する。

keybindingは固定Actionではなく、TUIのcommand promptと同じcommand列へ割り当てる。設定値は既定keybindingへのoverrideとして扱い、空文字で個別にunbindする。

```toml
[keybindings]
"ctrl-a g" = "focus left; zoom on"
"ctrl-a q" = ":attention ack; detach"
"ctrl-a x" = "" # 既定bindingを無効化
```

`;`はsingle/double quoteまたはbackslash内では引数の一部として扱う。command列は左から実行し、command prompt、確認dialog、copy modeのいずれかへ遷移した時点で後続commandを実行しない。keyは`ctrl-a`/`ctrl+a`/`c-a`/`^a`、Alt/Meta、単一文字、空白・編集・方向・Home/End・Page Up/Down・F1〜F12を受け付ける。未知command、不正key、同一byte列の重複、別bindingのprefixになる曖昧なsequenceはTUI起動時に拒否する。

shellとeditorは実行ファイル名だけでなく任意のargvを保持する。Ariadneはeditor固有のoptionを補わず、設定したargvをそのまま実行する。

```toml
[commands]
shell = ["/bin/zsh", "-l"]
editor = ["nvim"]
```

`commands.shell`未指定時は従来の`shell = "..."`、`$SHELL`/`COMSPEC`、OS既定shellの順に解決する。`commands.editor`未指定時は`$VISUAL`、`$EDITOR`、Unixの`vi`またはWindowsの`notepad.exe`を使用する。`:edit [h|v] [-- ARGS...]`は設定editorのargvへ追加引数を連結し、新しいTerminal Paneで起動する。将来、merge message等の一時編集を要求する内部commandも同じeditor argvを使用する。

#### 自然終了後のPane保持

現行の「exit code 0でPaneとSessionを自動削除する」規則を変更し、自然終了したTerminal Paneを既定でlayoutに残す。

```text
running
  └─ process exit
       └─ exited Pane
            ├─ restart: 前回のLaunchSpecで再起動
            ├─ run: 新しいcommandへ差し替えて起動
            └─ dismiss: Paneと残存historyを削除
```

- exit code 0は`exited`として保持し、error Labelを付けない。
- 非0 exit、signal、PTY I/O異常は保持し、error/attention対象とする。
- `kill`は明示的な破棄操作としてprocess、Pane、Session/historyを削除する。
- `restart <pane-id>`は保存済みargv/CWDを使い、新しいTerminalIDを割り当てる。
- `run <pane-id> -- <command...>`を追加し、PTYを持たない、または終了・失敗・placeholder状態のTerminal Paneへ新しいLaunchSpecとTerminalIDを割り当てる。ToolPaneには適用しない。
- `run`のCWDは`--cwd`、Paneに保存された前回CWD、呼出元frontendのCWDの順に決定する。環境変数は実行を要求したfrontendから渡し、永続化しない。
- 新しいPTYの割当前に古いretained Session/historyを削除し、frontend側のVT stateも新規作成する。
- 起動失敗はPaneを`failed`として保持し、`run`または`restart`で再試行可能にする。

```toml
[terminal]
successful_exit = "retain" # retain, close
```

#### Phase 9完了条件

- TUIだけでPane分割、active Pane変更、zoom、1セルresize、終了Paneの再利用、Window/Workspace切替を行える。
- layout変更が複数frontendとdaemon再起動後に維持される。
- 正常exit後も最終画面を表示でき、`restart`、`run`、`dismiss`を選択できる。
- 同一Paneへの同時`run`/`restart`でPTYが二重作成されない。

### 11.3 Phase 10: Scrollback / copy / clipboard / search

状態: **実装済み**

libghostty-vtのscrollback、selection、paste APIを`internal/vt/libghostty`へ追加し、Terminal Paneの通常入力とは独立したcopy/search modeを実装する。

#### Copy mode

- `Ctrl-A [`でactive Paneのcopy modeへ入る。
- 行・ページ単位のscroll、先頭・末尾移動、選択開始、選択範囲の拡張、取消を提供する。
- 文字列検索と次・前の一致箇所への移動を提供する。
- 選択確定時はUTF-8 `text/plain`としてdaemon共有clipboardとOS clipboardへ書き込み、copy modeを終了する。
- `Ctrl-A ]`で共有clipboardをactive Paneへpasteする。libghostty-vtのpaste APIを経由し、bracketed pasteとKitty paste eventを適用する。
- copy/search中もPTY outputをdrainする。viewportを自動で末尾へ戻さず、新規出力の存在をstatus barへ表示する。

#### Clipboard protocolと共有

- libghostty-vt callbackを利用してOSC 52、iTerm2 OSC 1337 Copy、Kitty clipboard protocol（OSC 5522）を自動認識する。
- 初期対応はUTF-8 `text/plain`だけとし、画像、任意MIME、binary clipboardは扱わない。
- read/writeとも既定は`ask`とし、`deny`、`ask`、`allow`を設定可能にする。
- `ask`では要求元Pane、操作種別、protocol、byte数を表示し、「今回だけ許可」または「拒否」を選択する。process単位の恒久許可は、要求元processを安全に識別できないため導入しない。
- daemonはuser単位の有界なin-memory clipboardを持ち、複数Pane/frontend間でlast-write-winsにより共有する。内容は永続化、Core Snapshot/Event、log、diagnostic errorへ含めず、daemon終了時に破棄する。
- OS clipboardはAriadneによるcopy、paste、clipboard protocol要求時だけ同期する。OS clipboardを常時監視せず、frontend接続時にも既存内容を上書きしない。
- backend commandはshellを介さず直接起動し、timeout、最大byte数、UTF-8検証を適用する。
- SSH環境では外側terminalへのOSC 52を優先し、未対応・timeout時はdaemon共有clipboardへfallbackする。

backendの既定優先順:

```text
Windows          Win32 Clipboard API
macOS            pbcopy / pbpaste
Linux Wayland    wl-copy / wl-paste
Linux X11        xclip / xsel
SSH              outer terminal OSC 52
fallback         daemon in-memory clipboard
```

```toml
[clipboard]
read = "ask"              # deny, ask, allow
write = "ask"             # deny, ask, allow
max_text_bytes = 1048576
command_timeout_ms = 2000
```

#### Phase 10完了条件

- scrollback選択、検索、copy、pasteが通常PTY入力と混線しない。
- OSC 52、OSC 1337、Kitty clipboardの分割入力、上限超過、不正UTF-8を安全に処理する。
- macOS、Wayland/X11、Windows、SSH fallbackをbackend単位で検証する。
- deny時にclipboard内容がPaneへ返らず、ask/allow時にも内容がlogや状態ファイルへ残らない。

### 11.4 Phase 11: Stash / restore

状態: **実装済み**

実行中Terminalを停止せず表示layoutから外すstashを実装する。stashはfrontendの非表示状態ではなくdaemon/Coreが所有する共有状態とする。

- Pane単位のstash、stash一覧、元の位置または指定位置へのrestoreを追加する。
- Window単位stashは配下Paneの関係を保ったままWindowを通常表示対象から外す。
- stashed TerminalもdaemonがPTY outputをdrainし、Plugin/Label/attention監視を継続する。
- Pane、Terminal、layout所属を分離し、stashed Paneをlayout leafなしでCoreに保持できる不変条件を定義する。
- restore用に直前のWorkspace/Window、split位置、weightをhintとして保存する。元の対象が削除済みなら選択中Window、`default/main`の順にfallbackする。
- stashed PaneとWindowは状態ファイルへ保存し、daemon再起動後も`restart`可能なplaceholderとして復元する。
- 実行中、正常終了、異常終了、history退避済みの各状態をstash一覧で区別する。

確定した操作:

```text
Ctrl-A s                         active Paneをstash
Ctrl-A S                         stash一覧をstatus barへ表示
:stash-pane [PANE]               Paneをstash
:stash-window [WINDOW]           Windowをstash
:stash-list                      stash一覧を表示
:restore-pane PANE [WINDOW TARGET h|v]
:restore-window WINDOW [WORKSPACE]
ariadne stash pane|window ID
ariadne stash list
ariadne restore pane|window ID [options]
```

#### Phase 11完了条件

- stash後もprocessとPTY drainが継続し、restore時に欠落なくhistory/live outputへ再接続できる。
- Pane/Windowのstash状態が複数frontendとdaemon再起動後に一致する。
- restore先消失、同時restore、終了とstashの競合を決定的に処理する。

### 11.5 Phase 12: ToolPane

状態: **実装済み**

PTYを持たない`PaneTool`へ具体的な内容識別とfrontend rendererを追加する。CoreはToolの種類と最小限の永続状態だけを保持し、描画・入力処理はfrontendへ閉じ込める。

Tool descriptorの基本要素:

```text
Provider   組み込み機能またはPluginの所有者
Type       Tool rendererの安定した種類
Instance   同一種類の個体識別子
```

- TUI/GUIは`Provider + Type`でrenderer factoryを解決し、Paneごとにstatefulなcontent instanceを生成する。
- renderer不在時もCore stateを破棄せず、「renderer unavailable」を表示するfallback contentを使用する。
- 最初の組み込みToolはcommand palette、stash一覧、help、Workspace/Window一覧、agent status、diagnosticsとする。
- command paletteはCLIと同じ操作を呼び出し、独自のCore mutation経路を持たない。
- ToolPaneにも通常Paneと同じsplit、move、resize、zoom、stash、chrome指定を適用する。
- Tool stateは`StateVersion`と楽観的更新用`Generation`を持つ64 KiB以下のJSONとし、同じ`Provider + Type + Instance`を参照する複数Paneで共有する。
- CUIでは`ariadne tool new [options] TYPE`と`ariadne tool list`、TUI command promptでは`:tool TYPE [INSTANCE] [h|v]`を使用する。

#### Phase 12完了条件

- PTY/libghosttyに依存せず、複数種類のToolPaneを同じlayoutへ配置できる。
- statefile round-trip後に同じdescriptorを復元し、renderer不在でも状態を保持できる。
- Toolのpanic/errorがTUI全体、Core、他Paneを停止させない。

### 11.6 Phase 13: Agent / attention

状態: **実装済み**

既存のPlugin/Label基盤をagent作業の状態表示とnavigationへ接続する。

- waiting、completed、warning、errorを含むattention分類、severity、発生時刻を表現する。
- 次・前のattention対象へ移動し、必要ならWorkspace/Windowを切り替えて対象Paneをactiveにする。
- status bar、Pane frame、Workspace/Window一覧、stash一覧、agent status ToolPaneへ状態を集約する。
- acknowledgement操作を追加し、未確認attentionと履歴表示を区別する。
- 初期検出器は静的linkした組み込みPluginとし、外部Plugin ABIを前提にしない。
- 誤検出やPlugin停止がTerminal lifecycle、PTY drain、他Pluginへ影響しないよう、既存のqueue/timeout/source cleanup規則を維持する。
- attentionはdaemon実行中だけ保持し、statefileへ永続化しない。上限到達時は確認済みの古い項目、次に未確認の古い項目から破棄する。
- agent markerはAPCの`ESC _ Ariadne;1;<base64url(JSON)> ESC \\`とする。JSONは`key`、`class`、省略可能な`severity`と`message`を持ち、fragmented input、8 KiB上限、不正入力の無視を検証する。
- markerを一度認識したPaneのprocessがexit code 0で終了した場合だけagent completionを生成する。非0 exit、signal、PTY errorはagent認識の有無にかかわらずsystem attentionを生成する。
- `Ctrl-A a` / `Ctrl-A A`で次・前の未確認attentionへ移動し、`Ctrl-A m`で1件を確認済みにする。stashed PaneはCore上でrestoreせず、一時的な全画面previewとして開き、`Ctrl-A z`で元のfocusへ戻る。
- CUIでは`ariadne attention list`と`ariadne attention ack ID`を使用する。

設定可能な上限:

```toml
[attention]
max_entries = 1024
marker_bytes = 8192
plugin_queue_bytes = 1048576
```

#### Phase 13完了条件

- active/stashedを問わずattention対象へWorkspace横断で移動できる。
- Plugin無効化時にそのPlugin由来のattention表示だけが除去される。
- 同一Eventの重複通知、acknowledgement、Pane削除を一貫して処理する。

### 11.7 Phase 14: 描画・入力・設定の成熟

状態: **実装済み**

#### 14A: resource操作とTerminal lifecycleの統一

- [x] CLI/TUIの`list [pane|window|workspace]`と`delete [pane|window|workspace] ID`を統一し、省略時はWindowを対象とする。CLIはID必須、TUIは現在の対象を補う。
- [x] `list --json`は同期済みSnapshotを使用し、`revision`、`unit`、`entries`を返す。ToolPaneとstashed resourceも一覧に含める。Pane/Window数は実際に所属するresource数で、stashに残る移動元IDは所属として数えない。
- [x] `delete pane`はToolまたは停止済みTerminalのみ、`delete window`は空かつ非stashedのみ、`delete workspace`は空かつ最後のWorkspace以外のみ許可する。stashed resourceの移動元IDが削除される扱いは既存Coreの仕様を維持する。
- [x] `kill`はPTY停止のみとし、Pane、履歴、Launch設定を残して`run`/`restart`を可能にする。明示停止のexit reasonを`killed`とし、異常終了やagent完了通知として扱わない。
- [x] `close`/`C-a x`は停止＋削除を維持する。`dismiss`は`delete pane`の互換aliasとする。旧protocolの破壊的`kill_terminal`は維持し、`stop_terminal`と`delete_pane`を追加する。

#### 14B: Unicode対応の差分描画

- [x] grapheme単位のSurfaceとwide cellのtailを扱い、旧・新frame双方のwide footprintを含めて差分を出力する。結合文字、CJK、ZWJ emojiを検証する。
- [x] writerにはimmutableなSurfaceとCursorを渡す。差分の基準は最後に実際に書いたframeとし、未出力の中間frameはlatest-frame方式で置換する。
- [x] 初回、resize、baseline無効化、差分byte数がfull-frame以上の場合はfull-frameへ戻す。clipboard等のcontrol出力は別の非drop queueで順序を保ち、queueは64件/4MiBに制限する。
- [x] libghostty-vtのcursor shape/blink、underline variation/color、64byteを超えるcell graphemeをbridgeする。
- [x] clipboardのask待ちがVT writerを占有しても、描画は前回のscreenを使用し、resizeは再試行する。許可確認の操作をVT lockで止めない。
- [x] VT lockの競合時はpaste/mouseの意味付き入力をbounded queueで再試行し、後続の通常入力も同じ順序で保持する。VTのdevice replyはuser inputとは分離する。queueは64件/4MiBで、detach時に破棄する。

#### 14C: mode keymap・theme・status widget・CWD

- [x] keymapを`[keybindings.normal]`、`[keybindings.copy]`、`[keybindings.prompt]`へ分離する。通常modeはcommand列、copy/promptはmode内action列を使用する。旧flat形式はmigration案内付きで拒否する。
- [x] 重複、未知command/action、曖昧なprefixを起動時に拒否する。EscapeとVT arrow sequenceの共存には短い待機を設ける。確認modalのキーは固定する。
- [x] semantic theme tokenとfull/split罫線をstrict TOMLで設定可能にする。既定paletteと既定操作を維持する。
- [x] status bar左右のwidget配置、format、styleを設定可能にする。組み込みwidgetと直接argvで実行する外部widgetを用意する。widgetがゼロでもprompt/確認modalを表示する。
- [x] 外部widgetはfrontend内で非同期実行する。既定interval 5秒、timeout 1秒、stdout 4KiB、各widget同時1件/全体4件を上限とする。stdoutはescape/controlを除去して1行にする。エラー時は最後の正常値とmarkerを残す。
- [x] focus/CWD変更時の古い結果を棄却し、実行中processをcancelする。終了・timeout・出力上限ではUnix process group/Windows Job Objectで子孫processまで停止する。
- [x] CWDの優先順位はwidget指定、Tool provider/type指定、Pane kind指定、既定値とする。Terminalの既定はPane CWD、Toolの既定はfrontend起動CWDとする。
- [x] Terminal CWDはローカルかつ有効な絶対pathのOSC 7のみ採用し、remote hostや存在しないpathはLaunch.CWD/起動CWDへfallbackする。Toolは任意のfrontend CWD providerを使用できる。VT由来CWDは永続化しない。
- [x] `ariadne init`の設定templateに全設定項目とOSC 7 hook例を記載し、既存ファイルや利用者のshell設定を変更しない。

OSC 7 hook例（shellの既存hookへ手動で組み込む。非ASCII/特殊文字を含むpathはURI encodeする）:

```sh
# bash: 既存PROMPT_COMMANDと合成する
ariadne_osc7() { printf '\033]7;file://%s%s\033\\' "${HOSTNAME:-localhost}" "$PWD"; }
# zsh: 既存precmd_functionsへ追加する
precmd_functions+=(ariadne_osc7)
```

#### 14D: mouse・minimum size・入力framing

- [x] mouseの既定はonで設定からoffにできる。chrome/separator/statusはAriadneが所有し、非focus Paneへの最初のclickはfocusのみとする。
- [x] focus済みTerminalがmouse trackingを要求中ならlibghosttyのencoderでapplicationへ送る。Shiftまたはcopy modeではAriadneのselection/historyを優先する。trackingなしではwheelで履歴、dragでselection、Toolは任意handlerへ送る。
- [x] gestureのrouteをreleaseまで固定し、Pane/focus/layout/resize/zoom変更でcaptureをcancelする。applicationのdrag cancel時はreleaseも送る。
- [x] separator dragはfrontend内のlayout previewだけを更新し、release時に既存`ResizeSplit`を1回commitする。resize/layout変更時はpreviewを破棄する。
- [x] content最小寸法の既定は2列×1行で設定可能とし、実際のchrome/separatorを最小寸法へ含める。split/keyboard resize/dragで下限を守る。
- [x] 画面が足りない間だけfocus Paneを表示し、Core layoutと明示zoom状態を変更せず、拡大時に元の分割を復元する。
- [x] 狭いstatusはclock、pane title、brandの順で省略する。高さ1行では通常statusを隠すがprompt/確認は表示する。
- [x] UTF-8の確定済みIME入力とfragmented inputを扱う。bracketed pasteはkeybindingと分離し、commandとして解釈せず、上限超過や途中の対象変更では破棄する。
- [x] OSC 0/2 titleをlibghostty-vtから取得し、Terminalごとのfrontend metadataとして保持する。Coreの手動Pane名を上書きせず、永続化しない。BEL/ST終端、fragmented input、空titleでのclearを扱う。
- [x] `[tui] pane_title = "auto"`を既定とし、手動Pane名、OSC title、起動コマンド名の順で表示する。`"terminal"`はOSC優先、`"pane"`は従来のPane名のみとする。枠とstatusの`{title}`へ共通反映し、`{pane_title}`/`{terminal_title}`で別々にも参照できる。title cacheを4KiBへ制限し、control/escapeを除去して表示する。
- Pane選択overlay、hyperlink表示、GUI IME preeditは後続Phaseへ延期する。

#### Phase 14完了条件

- [x] randomized/fuzz testでfull-frameと差分frameのlibghostty表示結果を比較する。200×60の疎な更新ではbyte数を90%以上削減する。pending frameの置換でもcontrol出力が欠落しない。
- [x] keybinding/theme不正設定、CWD fallback、外部widget上限と古い結果の棄却、process tree停止、mouse capture、wide selection、極小画面からの復元をtestする。
- [x] `go test -race ./...`と`go vet ./...`を通す。Linux/Windowsへstatic libghostty付きでcross compileする。
- [x] macOS/LinuxのPTYでpaste、Unicode、mouse focus/drag、resize/復元、detach、OSC 52 ask中のresize/denyをE2E確認する。Windows実機でConPTY lifecycle、TUI描画/入力処理test、Job Objectによる子孫停止を確認する。
- [x] WindowsのConPTY内TUIでUnicode/paste、mouse separator drag、極小resize/復元、detachのE2Eを確認する。Core更新とfrontend描画の非同期性を考慮して、分割描画完了後にmouseを送信する。
- IMEの確定UTF-8入力はtest対象だが、各OSのIME UIを伴う手動操作は別途確認する。

2026-09-17検証結果: `go test -race ./...`、`go vet ./...`、差分renderer fuzz（5秒指定、12,790実行）を通過。最新版の単一static-linked binaryを使用し、macOS/LinuxのPTY E2EおよびWindows実機のCLI/ConPTY TUI E2Eを各3回通過した。Windows/Linux実機でもTUI test suiteを通過し、外部widgetの子孫process停止を確認した。IME UIの手動確認は未実施。

OSC title追加検証: OSC 0/2、BEL/ST、fragmented input、空title、手動名と表示設定の優先順位、枠/statusのUnicode表示、control除去、clipboard ask中の非blocking取得をtestした。全体race/vetおよびmacOS/LinuxのPTY E2Eを通過し、Windows実機では`cmd.exe`のnative console titleがConPTY出力のOSC 0を通じてTUI表示へ反映されることを確認した。E2Eでは狭いstatusによるtitle省略と区別するため、十分な幅で表示を検証する。

### 11.8 Phase 15: 外部Plugin初版 / GUI延期

状態: **外部Plugin初版を実装、GUIは延期**

#### 15A: 契約・永続管理

- [x] 独立したAPI v1 DTO、manifest、capabilityとall/Workspace/Pane/context scopeを定義する。内部Go型を外部SDK/ABIへ公開しない。
- [x] state directoryのplugins/へversion付きatomic registry、package、Plugin固有dataを分離する。
- [x] local directoryのinstall/update/uninstall/enable/disable/restart/grant/revokeをdaemon管理APIへ統一する。新規installはdisabled・未許可、updateはdisabledへ戻す。
- [x] 再起動でpackage・enable・grantを復元する。uninstallはPane/Tool state/dataを保持し、明示--purgeだけで削除する。registry破損は保全して自動起動を停止する。

#### 15B: Runtime・監視・操作

- [x] stdio Content-Length JSON-RPC 2.0、bounded queue、各種timeout、Plugin単位のprocess supervisorを実装する。
- [x] DLL/.so/.dylibは同じariadne executableの専用helperへロードする。C ABIはversion/struct_size、message/send/log/shutdown tableとcopy buffer契約を持つ。
- [x] Core参照/event、layout、Terminal lifecycle、PTY観測/入力、共有clipboardを明示APIでbrokerする。汎用Core command実行口は提供しない。
- [x] host発行context、全対象のscope検査、現在のWorkspace所属とstashの区別、source/provider固定、旧実行generation拒否、古いTerminal入力拒否を実装する。
- [x] protocol不正/crash/timeout/overflowでは対象だけ停止する。未完了要求と自身のLabel/attentionを掃除し、Pane/dataと永続enableを保持する。復旧は手動restart。
- [x] Unix process group/Windows Job Objectで通常の子孫processも停止する。API権限と障害隔離を提供するが、OS file/network sandboxは保証しない。

#### 15C: Command・対話・描画・管理UI

- [x] CLI/TUI prompt/keybinding/palette/help/completionのplugin run ID COMMANDを統一する。TUI commandは非同期でevent loopを継続する。
- [x] 起動元frontendへのprompt/confirm/editorを提供する。headlessはerror、cancel/detachで要求を取り消す。
- [x] editorは既定argvを使用し、通常layoutへ入れない一時stashed PTY Paneを全画面previewする。正常exitでtextを返し、cancel/異常exitでは破棄する。元preview/focus/zoomを復元し、一時Paneを永続化しない。
- [x] Pluginごとの完全セルSurface/style/cursor、frontend/viewごとのhost ID/generation、resize/close後の古いframe棄却、mouse/paste/入力順序を実装する。
- [x] namespace付きPlugin widgetを既存status配置/format/styleへ接続し、非同期取得・最後の正常値・error marker・古い結果棄却を適用する。
- [x] :pluginで組み込みPlugin manager ToolPaneを開き、install/update/uninstall/toggle/restart/grant/revokeと詳細を操作する。
- [x] API仕様、C/C++ native作例、Go process作例、設定templateを提供する。download/marketplace/自動更新は作らない。

#### Phase 15検証

レビュー修正: PTYClientを接続単位で再利用し、複数editor呼び出しの重複handler登録を防ぐ。frontend viewは明示open/closeで管理し、closeで解放、遅延render/inputでは再作成しない。runtime restartではviewを保持し、旧runtime入力は引き続き拒否する。同時view上限は`plugins.max_views`へ分離する。障害時の停止状態と理由は同じlockで公開する。

追加レビュー修正: 永続化可能サイズをCore mutationのcommit前に検査し、拒否したmutationはEventを公開しない。state StoreはSnapshotをdeep-copyしてcoalesceした後にencodeする。Plugin EventはAttention evictionのような副作用の削除対象もscope判定に含める。TUI command列は非同期Plugin commandの完了後に続行し、失敗時は後続commandを実行しない。

長時間frontend対話はprompt/confirm用`frontend.interact`とeditor用`frontend.editor`へ権限を分離する。commandからは同期実行し、対話待機をcommand期限から除外する。ToolPane inputからはboundedな非同期interaction sessionを開始して直ちにIDを返し、`interaction.result` notificationで完了を通知する。render/widgetからの開始は禁止し、frontend/view終了、Plugin停止・再起動・権限変更、timeoutでsessionを取り消す。

- [x] package/enable/grant/data/Tool state復元、disabled install/update、registry破損/保存失敗、uninstall保持/purgeをtestする。
- [x] scope外操作、共有Tool stateの全Pane検査、focus変更/移動/stash、context/source/provider偽装、権限取消後の旧要求をtestする。
- [x] process crash/hang/不正protocol/queue overflow、複数frontendと異なる寸法、Unicode wide cell、古いframe、pasteをtestする。
- [x] macOSの実daemonでprocess/native Plugin、Core scope、headless error、prompt、実PTY editor、ToolPane/widget、native stdout分離とcrash/hang/不正message隔離をE2E確認する。

- [x] 実daemon再起動でpackage/enable/grant/private data/共有Tool stateを復元するE2Eを通す。
- [x] 全体`go test -race -shuffle=on ./...`、`go vet ./...`とmacOS/Linux/Windows向けCGO・libghostty付き単一binary buildを通す。
- [ ] この追加実装のLinux/Windows実機でprocess/native/PTY/ConPTY E2Eを確認する。CIの3 OS integration suiteへ検証を追加済み。

GUIは引き続き最低優先順位で延期する。実装時はdaemon protocol clientとし、CoreとTerminal lifecycleを再実装せず、TUIと同じSnapshot/Event、SplitID、Tool descriptor、clipboard policyを利用する。GUI固有font/GPU/tab/window/system clipboard/notificationはfrontendへ分離する。

### 11.9 今後の実装順序

15. [x] TUI command/mode/keymap基盤とPane/Window/Workspace操作を実装する。
16. [x] SplitID、永続resize、zoom、active Pane操作を実装する。
17. [x] 正常exit時のPane保持と`run`/`restart`/`dismiss` lifecycleを実装する。
18. [x] libghostty-vtのscrollback/selection/paste境界とcopy/search modeを実装する。
19. [x] daemon共有clipboard、OS backend、OSC/Kitty protocol、ask/deny/allow policyを実装する。
20. [x] Pane/Windowのstash、一覧、restore、永続化を実装する。
21. [x] Tool descriptorと組み込みToolPaneを実装する。
22. [x] agent attention、acknowledgement、Workspace横断navigationを実装する。
23. [x] 差分描画、theme、設定可能keybinding、mouse、terminal互換性を実装する。
24. [x] 外部Plugin初版（process/native helper/監視/command/ToolPane/widget/永続管理）を実装する。
25. [x] TUI版v1.0の配布・互換性・検証基盤を実装する。
26. [ ] Windows GUIをdaemon protocol clientとして実装する。

### 11.10 Phase 16: TUI v1.0 release

状態: **実装・公開検証済み（`v1.0.0`）**

- [x] 単一binaryへversion、commit、build日時を埋め込み、`--version`と`version`で表示する。
- [x] macOS/Linux/Windowsのamd64/arm64をnative GitHub runnerでbuild/testする6環境matrixを用意する。Windows arm64以外はrace detectorも通す。
- [x] macOS 13、Ubuntu 22.04相当のglibc 2.35、Windows 10 1809をv1の最低動作環境とする。
- [x] archiveへbinary、README、MIT LICENSE、第三者notice/licenseを収録し、GitHub ReleaseへSHA-256 checksumとartifact attestationを公開する。
- [x] GitHub Actionsを完全commit SHAへ固定する。配布binaryはv1.0時点ではOS code signing/Apple notarizationを行わず、その旨をREADMEへ明記する。
- [x] Plugin API v1、公開CLI/configを1.x内で互換維持する。stateはforward migrationのみ、daemon/frontendは同一versionを原則とする。
- [x] macOSで全race/vet、実daemon/PTY/process/native Plugin E2E、配布archiveを確認する。
- [x] Linux amd64実機でstatic libghostty付き候補のPTY/CLI/TUI E2Eを確認する。
- [x] Windows amd64実機でstatic libghostty付き候補のCLI/ConPTY/TUI E2Eを確認する。
- [x] repositoryをpublicへ変更し、`v1.0.0-rc.3`を公開する。6環境CI、artifact、checksum、attestation、取得後の起動を確認する。
- [x] RC確認後に同一工程で`v1.0.0`を公開する。

v1.0にはGUI、installer、package manager配布、SBOMを含めない。GUIは次Phaseで同じdaemon protocol/Core modelを利用して実装する。
